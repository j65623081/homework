<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Файл-хранилище заметок
    |--------------------------------------------------------------------------
    |
    | Единственное хранилище сервиса — один JSON-файл на диске. Базы данных нет,
    | это условие задания. Путь вынесен в конфиг, чтобы тесты могли подменить его
    | на временный файл и не трогать рабочий.
    |
    */

    'path' => env('NOTES_STORAGE_PATH', storage_path('app'.DIRECTORY_SEPARATOR.'notes.json')),

    /*
    |--------------------------------------------------------------------------
    | Реестр ключей идемпотентности импорта
    |--------------------------------------------------------------------------
    |
    | Отдельный файл, рядом с хранилищем заметок. Отдельный он потому, что формат
    | notes.json не меняется: реестр — служебные данные импорта, а не заметки.
    | Путь вынесен в конфиг по той же причине, что и путь к заметкам, — тесты
    | подменяют его на временный файл.
    |
    */

    'keys_path' => env(
        'NOTES_IMPORT_KEYS_PATH',
        storage_path('app'.DIRECTORY_SEPARATOR.'import_keys.json')
    ),

];
