{
  "name": "notes-api",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dependency HTTP API for notes with tags",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  }
}
